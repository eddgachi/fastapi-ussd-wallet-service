

Features to Develop
Core Features (MVP)

    User Registration & Authentication via USSD

    Loan Application through USSD menu

    Loan Eligibility Check (basic rules-based)

    M-Pesa Disbursement integration

    Wallet System for loan management

    Transaction History via USSD

    Session Management for USSD flows

Enhanced Features

    Admin Dashboard (web-based for monitoring)

    Loan Repayment via USSD

    Payment Reminders (SMS/Celery tasks)

    Credit Scoring (basic algorithm)
    
    

    
# Generate initial migration (if this is your first time)
docker compose exec web alembic revision --autogenerate -m "Initial migration"

# Then run the migration
docker compose exec web alembic upgrade head


# Check migration history
docker compose exec web alembic current

# Check if tables were created (connect to PostgreSQL)
docker compose exec db psql -U fastapi_user -d umoja_loans -c "\dt"


# Check wallets
docker compose exec db psql -U fastapi_user -d umoja_loans -c "SELECT user_id, available_balance, loan_balance FROM wallets;"

# Check users
docker compose exec db psql -U fastapi_user -d umoja_loans -c "SELECT phone_number, first_name FROM users;"

# Check farmers
docker compose exec db psql -U fastapi_user -d umoja_loans -c "SELECT registration_number, full_name FROM farmers;"
